@model IEnumerable<Nhom9_WebsiteGiaySneaker.Models.Brand>

@{
    ViewBag.Title = "Brand";
}

<ul class="list-group list-group-flush text-left">
    <li class="list-group-item" style="background-color:darkorange;">
        <a href="#" style="text-transform:uppercase; text-decoration:none; color:white; font-weight:bold;">Chủ Đề Sách</a>
    </li>

    @foreach (var item in Model)
    {
        <li class="list-group-item" style="text-transform:uppercase;color:blue">

            <a href="@Url.Action("SanPhamTheoBrand","SanPham", new {@maB=item.MaBrand })" style="text-transform:uppercase; text-decoration:none;">@item.TenBrand  </a>

        </li>
    }

</ul>